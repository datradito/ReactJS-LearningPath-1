/* import { usContext } from "../../src/base-pruebas/06-deses-obj";

describe('Pruebas en 06-deses-obj' , () => {

    test('usContext deberia retornar un objeto', () => {
        
        const clave = "Paloma";
        const age = "20";

        const context =  usContext(clave, age)

        const testContext = {
            nombreClave: clave, 
            anios: age,
            latlog: {
                lat:14.1232,
                lng: -12.3232
            }
        }

        
        expect(testContext).toEqual(context)

    });
}) */